{
  "workspaces": [
    "packages/*"
  ],
  "catalog": {
    "@types/bun": "1.4.0"
  },
  "peerDependencies": {
    "typescript": "^7.0.2"
  },
  "devDependencies": {
    "@ianvs/prettier-plugin-sort-imports": "^4.7.1",
    "prettier": "^3.9.6"
  },
  "scripts": {
    "format": "prettier --write .",
    "test": "bun test --cwd packages/lib && bun test --cwd packages/cli"
  }
}
